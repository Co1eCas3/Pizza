import React from 'react';

import './User_Sidebar.css';
import themes from '../../../utils/themes';

const userSidebar = (props) => {
  const sbStyles = {
    backgroundColor: themes.colors.settings.bg
  }

  return (
    <div className='.user-sidebar' style={sbStyles}>

    </div>
  )
}